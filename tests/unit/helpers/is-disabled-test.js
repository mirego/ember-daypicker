import { isDisabled } from '../../../helpers/is-disabled';
import { module, test } from 'qunit';

module('Unit | Helper | is disabled');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isDisabled(42);
  assert.ok(result);
});
